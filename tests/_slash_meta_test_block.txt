// Reusable block inserted by the /help + /commands review pass.
// Adds (a) a meta-test pinning that every KNOWN_COMMANDS entry is parseable,
// and (b) stronger content assertions on formatHelp (named rows + line count).
